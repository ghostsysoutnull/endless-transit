/** A scene asks for its child to be entered: a bubbling event whose detail is `{ id }`, the option's id. */
export const PICK = 'pick';
/** A child of the scene is pointed at (or no longer is): a bubbling event whose detail is `{ id }`, empty for none. */
export const LIGHT = 'light';
