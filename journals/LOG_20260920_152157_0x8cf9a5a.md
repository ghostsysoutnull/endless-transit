# [OBSERVER_LOG_0x8cf9a5a] | 2026-09-20

## 🌌 THE NARRATIVE (The Vibe)

> The manual spoke in the voice of the web, and the web had moved on without telling it. It told new Observers they would wake at the root of the universe; they wake on a street. It told them to leave their Keystone on the floor of a cell for safekeeping; the cell keeps a name and nothing else. It promised that door traces could be read for free, that a link at zero was a link lost, that the Minor Cultures lived in borrowed Monolith walls. Each line had been true once, or had never been checked.
>
> So every sentence that claimed a number, a key or a rule was walked back to the code that decides it. The pages were corrected in their own voice. Then the corrections were handed to readers who had not written them, with one instruction: assume this is wrong. They found seventeen places where it was — among them a survival number, written on four pages in a single afternoon, that would have sent Observers into the Substrate believing the arteries cost half what they do.
>
> The archive of the builders' own arguments — forty retros and plans — had been hanging in the public gallery, unlinked and unthemed. It is back in the workshop. A lost address now answers `SIGNAL_LOST` instead of a stranger's error page.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** GitHub Pages audit (user request: "review all pages on gh pages … stale or wrong data") — **COMPLETE**, docs only, no `src/` change. Branch `docs/gh-pages-audit` merged `--no-ff` as `8cf9a5a`, pushed; post-push record on `docs/close-gh-pages`. Plan + execution record + deviations: `tasks/completed/GH_PAGES_AUDIT_PLAN.md` (grill: AMEND, 21 items → applied).
- **MASTER_SEED:** `4660` — walked headless from a scratch directory (player files untouched). Start: street *Busy Terrace* (`0.0.0.0.0.0.0.0`), Coherence 99% on the first frame; building `1` *Eternal Shaft* (3 floors) → `0` → `c` → `1` = *Unbroken Fuel Depot*, 15 objects; `oscilloscope` captured at **1108 Hz** (84 × 12 × 1.1, truncated) — now the codex's worked Gematria example.
- **User decisions:** session scope = the pages only; game bugs to the backlog **below HK-022** (repeated once as a correction); 5-file cap lifted for this wave; one review at the end → commits per area; D1–D4 accepted (keep two manual pages with one owner per fact, rewrite `system_specifications` and `lip_addressing` in place, spoiler warnings).

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [GH_PAGES_AUDIT]**

| Commit | Change | Evidence |
| :--- | :--- | :--- |
| `0e0e94a` | plan (grilled) + HK-023 logged, HK-021 extended, lesson | grill opened ~40 agent-reported citations: 29 held, **4 false** (three would have replaced true text: drift "one in twenty", `STABILITY` = drift odds, "every container has a map"), 8 overreached |
| `02976d8` | `_config.yml` excludes `analysis`, `retro`, the OO problem list, `docs/README.md` | live before: 200 for all; after the push: 404 |
| `5d30cb4` | install guide + player's guide | old manual launch command crashed on frame 1 (no `src/main/resources`); `run.sh --test` targets a missing file; guide said "start at the universe", "never reads its arguments" (contradicting its own `:363`), "stash a Keystone". Guide lines 1–103 kept their numbering (a test comment cites `:102-103`) |
| `e0b24b3` | rest of the manual; `system_specifications` rewritten against the code | `locus.branch()` not a `Random` scrambler; pane 86 not 88; what the save really holds |
| `7803517` `9ffef70` | codex + Atlas; `lip_addressing` rewritten; `[CLEARANCE_WARNING]` on the four endgame nodes | archetypes now formable from the lexicons; Echo roll is seeded (HK-015); "up to three" upper strata |
| `0085112` | fixes from the independent verification pass | two fresh agents over the diff: **3 false, 12 overreach, 2 contradictions** in text written this wave — worst: "arteries cost 2" on four pages (after `c` the player still stands on the Layer → 4 under an Entropic world) |
| `e425dc7` | site shell: current-page nav, meta description + OG, inline favicon, `404.html`, CSS (flicker never ran — invalid keyword; undefined `--primary-color`; header wraps on a phone) | the only new Liquid (`if page.url`); 9 `if` / 9 `endif`; Pages build `built`, no error |
| `982c99f` | index: "Last updated at" line (user request) | live |
| `1c164b3` `d3ea648` `e4172cb` | post-push record; lesson + **WF-010** from the user's second correction; stale facts found on the way: two seeding lessons still taught the `Random` scrambler, `ui/CLAUDE.md` said panes 88 / 38 | — |

**Logged, not fixed (HK-023, below HK-022 by user decision):** a save made below the Bedrock does not restore (two read-only probes: the Layer's LIP resolves to null on a regrown web, then a null dereference at `PersistenceService.groovy:59` — a suite test is step 0); `Building.groovy:49,51,150` calls `ui.Terminal.clock` by fully qualified name (the lint rule `ModelNeverImportsUi` cannot see it); map colours `"RED"` / `"WHITE"` print as words; the resonance tally is not saved and can be farmed by drop / re-take; `run.sh --test` and `--headless` are dead; README says Groovy 4.x, `build.gradle` pins 5.0.0.

**Where the errors came from:** one fact, several copies, one copy fixed. HK-014 wrote "Minor Cultures borrow Monolith geometry" into three places; HK-016 made it false the same day and closed without revisiting the pages. The RESONANT_TRACES rule was wrong on five pages. Hence the plan's Step 2 (fix by fact, not by page) and one owner page per rule (`operational_protocols`).

`/close-wave`: Light (no `src/`) + Player docs + Lessons, run before the merge; re-run after the push with Chronicle added (law: `ui/CLAUDE.md`). The first run ended in a "leftovers" list and the user had to ask why — **WF-010**: the command has no post-merge step.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 282 / 282 / 0 / 0 | `LINT=PASS` 220 files | scan seed 0 → 9 | 36 goldens unchanged | `DOCS=PASS` — none of them could move; run as proof.
- **SITE_STATE:** 19 pages / 271 links all 200; internal records 404; build `8cf9a5a` `built`. **Unverified:** the header at phone width (no browser in the session).
- **NEXT_PHASE:** user decision — **HK-022** first (`NameGenerator` statics), then HK-021, HK-023, HK-013, O1; WF-010 (Medium) and WF-006 (Low) at the next workflow review.

---
*End of Fragment. Neural Trace Stabilized.*
