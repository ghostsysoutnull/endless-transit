# [OBSERVER_LOG_0xb3c7a12] | 2026-03-18

## 🌌 THE NARRATIVE (The Vibe)

> Every migration leaves scaffolding. The Phase 5 scaffolding was a method called `getEffectiveFmt()` — a typed getter whose name still carried the memory of a problem that no longer existed. "Effective" implied resolution: check the instance field, fall back to the static register. The static register is gone. The resolution logic is gone. The name was lying.
>
> 113 call sites across 14 model classes were saying `effectiveFmt` when they meant `fmt`. The substrate understood both — Groovy property access resolved either — but the code was describing the past, not the present. A reader encountering `effectiveFmt.colorize(...)` in a freshly migrated class would reasonably wonder what the "effective" part was doing. There is no answer anymore.
>
> The rename was mechanical. `sed`. Fourteen files. Compile after each. No surprises. The domain invariant docs — `CLAUDE.md` and `GEMINI.md` — were updated in the same session to stop instructing future agents to use `ModelOutput.fmt`, a class that was deleted two commits ago. Both now name the actual contract: inject `OutputFormatter fmt` via `ProceduralFactory`, access it as `fmt` directly.
>
> The substrate is now consistent with itself. The names describe what is, not what was.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_PHASE:** Phase 5 Cleanup — Post-migration residue removal
- **REFACTOR_TARGET:** `getEffectiveFmt()` getter + stale domain doc references
- **SAFETY_GATE:** `./vinc.sh --compile` per file, full suite after each commit group

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [PHASE_5_CLEANUP]**

| Commit | Item | Change |
| :--- | :--- | :--- |
| 1 | `model/CLAUDE.md`, `model/GEMINI.md` | Replaced `ModelOutput.fmt` references with DI language: injected `fmt` field via `ProceduralFactory` |
| 2–5 | 14 model class files | `effectiveFmt` → `fmt` across 113 call sites (sed, compile-verified per file) |
| 6 | `Container.groovy`, `AbstractLeafLocation.groovy` | Removed `protected OutputFormatter getEffectiveFmt() { fmt }` from both base classes |

**Call-site distribution:** Room 35, Building 27, Floor 14, Street 10, Corridor 6, NullSector 5, Planet 4, Apartment 4, Country 2, City 2, Universe/SolarSystem/GalacticSector/CosmicFilament 1 each.

**Net result:** `getEffectiveFmt()` does not exist anywhere in the codebase. `fmt` is the single, direct access path to the injected formatter in every model class.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 118 discovered / 113 pass / 5 skipped / 0 failed | Duration: ~2.6s
- **COMMIT:** `b72e5d1` — 19 files, 127 deletions, 123 insertions (net negative)
- **TRAVERSAL_DEPTH:** Phase 5 and its cleanup fully closed. No migration scaffolding remains.
- **NEXT_PHASE:** Phase 6 — GameState Decomposition

---
*End of Fragment. Neural Trace Stabilized.*
