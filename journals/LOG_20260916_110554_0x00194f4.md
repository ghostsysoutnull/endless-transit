# [OBSERVER_LOG_0x00194f4] | 2026-09-16

## 🌌 THE NARRATIVE (The Vibe)

> For six months the scribe had a page it never wrote on. *Discovery* — the first footfall on a new street, a new city, a tower never entered — was a line the journal knew how to write and nobody asked it to. The ticker's `LOC:` rows stayed dark. The session summary counted "macro-locations mapped" and the number was always zero. Not broken. Unspoken.
>
> The cause was a March persistence commit that moved footprint tracking into the player and, in the same gesture, dropped the sentence that told the scribe. The player kept remembering where it had been. It just stopped saying so.
>
> This session gives the player the sentence back — but not the old way, reaching across to the journal by name. The player already has a voice on the bus since Phase 10. Now, when a path is new to its memory, it says *I discovered this place*, once, and only for places large enough to matter. The scribe, already listening for captures and syntheses, listens for that too and writes the line it always could.
>
> The first thing the pins showed was the shape of the silence: four contracts, all red, all for the same reason — nothing spoke. Then three files changed and all four turned green.
>
> The frames moved, on purpose. Six goldens now carry a `LOC:` line where an empty row used to be: the tower, discovered, from the moment the player steps into it. The other thirty are byte-identical. The starting street still never appears — the session begins after the world is built, and the harness now knows that too.
>
> One thing the pane cannot hide: it is thirty-seven characters wide and the world's paths are longer. Every discovery reads *Universe > … > [VOID] > Lam…* That is the March contract, restored exactly, and named here so the next decision about the ticker is made with open eyes.
>
> The substrate now records where the player has been. The journal is complete again.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** Housekeeping HK-010 — restore discovery journaling (behavior change, user decision 2026-09-16)
- **REFACTOR_TARGET:** `core/LocationDiscovered.groovy` (new), `core/Player.groovy` (`markFootprint` publishes), `core/JournalManager.groovy` (`attach` subscribes `logDiscovery`)
- **SAFETY_GATE:** `DiscoveryEventContractTest` (4 pins, RED → GREEN), `JournalTest`, `JournalEventContractTest`, 36 golden frames (6 regenerated and reviewed, 30 byte-identical), `./vinc.sh --scan` seed 0 → 9 nodes
- **MASTER_SEED:** `12345` (golden and contract seed)

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_010_DISCOVERY_EVENTS]**

| Commit | Item | Change |
| :--- | :--- | :--- |
| `6590c97` | Plan | `tasks/HK_010_PLAN.md` — evidence, coverage table (three UNGUARDED rows), five declared edges, predicted golden diff. `/grill`: all six PASS; two citation line numbers corrected → CLEARED. |
| `06ed2d4` | Code | `LocationDiscovered` (`@CompileStatic`, `location`, `path`, `lip`). `Player.markFootprint`: `if (isMacro && visitedPaths.add(path)) events.publish(...)`. `JournalManager.attach`: third typed subscription → `logDiscovery(e.path, e.location)`. `DiscoveryEventContractTest`: D1 publisher contract (once per new macro path, never for a Floor), D2 exact `[DISCOVERY] <path> [Era: …, Resonance: …]` line, D3 re-entry/Floor idempotence, D4 `[LOC]` manifest line + `Network Expansion: 1`. `HudFrameHarness`: `JournalManager.reset()` moved *after* `new Game(seed)` (mirrors `start()` → `startSession`). Goldens 13–18 regenerated: first ticker line only. |
| `4967266` | Docs | Backlog HK-010 → CLOSED; `core/CLAUDE.md` events bullet (three events, start-locus wipe); OOA plan Phase 10 note; plan → `tasks/completed/`. |
| `00194f4` | Merge | `--no-ff` to `master`, pushed. |

**Pre-change RED (recorded):** `markFootprint_publishesOncePerNewMacroPath` expected 1 got 0; `enteringBuilding_journalsDiscoveryLine` expected 1 got 0; `reentryAndFloor_addNoDiscovery` expected 1 got 0; session file showed `Network Expansion: 0`.

**Golden diff before the harness move:** 8 frames (01, 02, 13–18) — 01/02 carried the constructor's Street/City/Planet discoveries, which production wipes in `startSession`. After the move: exactly the six predicted files, one line each.

**Declared edges:** E1 starting locus never journaled (March order preserved); E2 memento `restore()` re-discovers (test-only path; `restoreSession` refills `visitedPaths` and does not); E3 37-char ticker truncation (`Terminal.splitBoxedLine`, right pane) — not changed; E4 `JournalEventContractTest` unaffected (`takeRight(1)`); E5 standalone `Player` has an inert bus. Name deviation: `LocationDiscovered`, not the backlog's `LocationEntered` — it fires once per new path, and the ancestor loop discovers a City/Planet the player never entered.

**Observed, not touched:** `tasks/backlog/HOUSEKEEPING.md` CLOSED section carries duplicate entries for HK-001..006 (pre-existing before this session).

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 207 discovered / 207 pass / 0 skipped / 0 failed | Duration: ~3.0s
- **TRAVERSAL_DEPTH:** HK-010 closed and merged. Housekeeping open: HK-008, HK-009, HK-011. O1/O2 optional.
- **NEXT_PHASE:** user decision — O2 CodeNarc (recommended next), HK-009 (10-minute cleanup), HK-008/HK-011 (static singletons; HK-011 now also owns the ticker `LOC:` truncation question).

---
*End of Fragment. Neural Trace Stabilized.*
