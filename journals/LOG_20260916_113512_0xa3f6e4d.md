# [OBSERVER_LOG_0xa3f6e4d] | 2026-09-16

## 🌌 THE NARRATIVE (The Vibe)

> One door was left standing after the registry took over. Twelve "fill me in" delegators had gone with their callers in HK-005; the thirteenth stayed because two tests still knocked on it by name. A small thing, on the list to clear before the next phase.
>
> The plan read the two tests line by line, as the protocol demands, and the small thing turned out to have a shadow. Tap the factory on the shoulder and say *fill this apartment now*, and it does — and halfway through, while it is counting the rooms it just made, the apartment notices nobody has filled it yet and fills itself. Two sets of rooms. Two object pools. Every count even. Neither test had ever asked for an absolute number, so the doubling lived in the suite for months without a single red.
>
> The grill caught a claim in the first draft — that the corridor's back-propagation had already populated the apartments — and it was wrong: the corridor only reads the first room's category off the seed. It never touches the rooms. The mechanism was the same nested self-population in both tests; the correction made the finding cleaner, not weaker.
>
> Before deleting anything, a script mirrored both tests with and without the explicit call: 544 rooms across fifty apartments with it, 272 without. Every explicit count even, every lazy count honest. Then the door came down, and the two tests now do what production does — look at the rooms and let the apartment fill itself once.
>
> The facade has one way in now. The substrate is a little quieter.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** Housekeeping HK-009 — delete `ProceduralFactory.populateApartment`
- **REFACTOR_TARGET:** `procgen/ProceduralFactory.groovy` (−7 lines); `ObjectDistributionTest`, `ProcgenVariabilityTest` (one call removed each); `ProcgenDeepSnapshotTest` (three text tokens)
- **SAFETY_GATE:** `ProcgenDeepSnapshotTest.apartment_vibeMatchAndObjectPool_pinnedForSeed0x1234` (lazy single population pinned), the two migrated tests' variance assertions, 36 goldens (untouched), `./vinc.sh --scan` seed 0 → 9

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_009_POPULATE_DELEGATOR]**

| Commit | Item | Change |
| :--- | :--- | :--- |
| `05ec641` | Plan | `tasks/HK_009_PLAN.md`. `/grill`: check 5 AMEND — the draft said `CorridorFactory.populate` already populates apartments; it re-derives the first room's category from `aptLocus.branch(0)` and never reads `rooms`. Corrected → CLEARED. |
| `6a7c813` | Code | Delegator + its comments deleted; javadoc line updated. Two tests read `apt.rooms` lazily. Deep snapshot tokens renamed so `grep populateApartment src/` → 0. Suite 207/207/0/0. |
| `d0a5e6c` | Docs | Backlog HK-009 → CLOSED; blueprint line dropped; plan → `tasks/completed/`; todo/recovery refreshed (O2 note: `vinc.sh` never invokes Gradle — reshape as `./vinc.sh --lint`). |
| `a3f6e4d` | Merge | `--no-ff` to `master`, pushed. |

**Step 0 (scratchpad script, same classpath as the tests), recorded:**
- explicit call: `VARIABILITY roomCounts=[14, 12, 20, 10, …] allEven=true`; `DISTRIBUTION totalRooms=544 … allEven=true`
- lazy only: `VARIABILITY roomCounts=[7, 6, 10, 5, …] allEven=false`; `DISTRIBUTION totalRooms=272 … allEven=false`

**Mechanism:** `ApartmentFactory.populate` adds rooms via `addLocation` (no lazy trigger), then reads `a.rooms.size()` for
distribution — the first lazy access — which calls `ensureChildrenPopulated()` → `populate(Container)` → a nested
`ApartmentFactory.populate`. Test-only: production only ever reaches `populate` through the lazy gate, whose flag is already set.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 207 discovered / 207 pass / 0 skipped / 0 failed | Duration: ~3.2s
- **TRAVERSAL_DEPTH:** HK-009 closed and merged. Housekeeping open: HK-008, HK-011. O1/O2 optional.
- **NEXT_PHASE:** user decision — O2 CodeNarc (reshaped as a `vinc.sh` lint mode), HK-011 (journal instance + ticker `LOC:` line), HK-008, O1.

---
*End of Fragment. Neural Trace Stabilized.*
