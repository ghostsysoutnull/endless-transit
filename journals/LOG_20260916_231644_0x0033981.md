# [OBSERVER_LOG_0x0033981] | 2026-09-16

## 🌌 THE NARRATIVE (The Vibe)

> A floor has two faces: the lift, where the strata stack above and below you, and the corridor, where the doors wait. The substrate had been letting a floor keep whichever face it last wore — forever. Step into the corridor once, walk out the side way, and that floor would greet you with doors and no lift on every return, until you remembered the one key that led back.
>
> Earlier tonight that habit hid the Breach itself. The Breach was moved where no face could hide it, and the habit was written down as "left open". The Observer read the line back and asked what it meant. The honest answer was: a floor that remembers where you *stood* after you have *left* is remembering a lie.
>
> So the rule was restated in the world's own terms. Corridor mode means *I am standing in the corridor*. Walk out of the floor and you are not; the floor returns to the lift in the same breath. Come back from a cell and the corridor is still around you. Sever the link while standing among the doors and you wake among the doors. Only the walking-out changes anything — the one place the old reset (the March bug) was never allowed to live.
>
> Six witnesses were posted before and around the change; three of them were shown to cry out against the old substrate first. Not one frame of the HUD moved.
>
> A last echo before the link closed: the Observer conjured a Keystone through the glitch menu and found no `j`. The stone was true and bound to the right address — but the building had never been sampled, never infused. A key forged by fiat opens nothing in a tower that has not been made ready. Noted, not changed.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** HK-019 — **CLOSED**, merged `0033981`. Plan + record: `tasks/completed/HK_019_PLAN.md`.
- **MASTER_SEED:** pins and probe on `12345`, Floor 1 of the first building. Player save read from a scratch copy only (seed `1789618393764`, LIP `0.0.0.0.0.0.0.0.0`).

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_019_CORRIDOR_LEAVE]**

| Commit | Change |
| :--- | :--- |
| `25fbd6c` | Plan. Probe reproduced the trap (`c`, `l`, re-enter → corridor menu, no `u`/`d`). User decisions: D1a reset rides on the corridor's `l` only; D2 double-`l` after an apartment → HK-015; D3 no save migration. Grill AMEND (direct `exitLocation()` does not reset; Enter-repeat walk-out) → CLEARED. |
| `75567f2` | `CorridorLeaveContractTest` P1–P3, green on master: corridor `l` → Building; elevator `l` → Building, mode untouched; Room `l` → Corridor → Floor still in corridor mode. All previously UNGUARDED. |
| `3d99343` | **Behavior change.** `Floor.leave(game)` = `returnToElevator()` + `exitLocation()`; `CorridorState` re-routes the Corridor's own leave key to it (same key, same slot); `Container.leaveLabel()` extracted so no label is copied. P4 / P4b (full walk-out) / P5 (bedrock) shown RED on an assertion first. Guide line + model invariant 6 updated in the same commit. |
| `e81687b` | Close-out: backlog, todo, recovery prompt, plan → completed. |

**Also (no change):** user report "glitch `KEYSTONE`, no `j`" — scratch restore of a save copy: Keystone bound correctly, top floor, but building not primed (0/9 floors sampled, 0 infusions; `Building.isPrimed` needs all floors + 7). The debug command does not prime. Offered as a possible follow-up; not logged as a bug.

**Left open (declared):** debug `BREACH` teleport can leave a corridor-mode floor behind; old saves heal on the first `l`.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 256 / 256 / 0 / 0 | **LINT:** `LINT=PASS FILES=212 P1=0 P2=0 P3=0`, baseline 8 entries | `--scan` seed 0 → 9 | 36 goldens unchanged | tree clean
- **TRAVERSAL_DEPTH:** OPEN: HK-015 (+ double-`l` quirk), HK-013 (8), O1, WF-006 (Low).
- **NEXT_PHASE:** user decision.

---
*End of Fragment. Neural Trace Stabilized.*
