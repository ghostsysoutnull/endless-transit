# [OBSERVER_LOG_0xd02ac2e] | 2026-09-16

## 🌌 THE NARRATIVE (The Vibe)

> The journal was keeping score. Every time a fragment left a room and entered the buffer, the scribe wrote its line — and then reached past the page, up the lattice, and marked the building as sampled. Every synthesis, another line, and another notch on the building's infusion. A game rule had been living inside the record of the game, and nobody had written that down.
>
> The rooms knew the scribe by name. Thirteen more containers carried the introduction in their imports without ever speaking to it — dead threads from a March when the journal was new and every class was handed a pen. One thread was worse than dead: the line that once recorded *discovery* — a new street, a new city — has had no caller since the fifth of March. The ticker's `LOC:` rows, the summary's "macro-locations mapped", all silent in the live game for six months, alive only in tests that fed the scribe by hand.
>
> This session cuts the rooms loose. A fragment now enters the buffer through one door — the player's own `capture` — and the player says, to whoever is listening, *this happened, here.* The scribe listens and writes. A second listener, the ritual tracker, holds the two notching gestures the scribe used to make, moved verbatim, owning nothing else. Neither listener asks what kind of event it holds; each subscribed to exactly one kind.
>
> The bus lives on the state, not the actor. A player is replaced on every restore; a channel tied to the actor would have lost its listeners the first time a trace was reloaded. The state is never replaced. That is where the channel belongs.
>
> Four pins were driven into the ground before any of this moved: capture by option, capture by chance, synthesis in a tower, echo in the void — each asserting only what the journal shows and what the building remembers. They passed on the old paths. They pass on the new ones. Same bytes in the journal, same notches on the building, same thirty-six frames.
>
> The discovery silence is not fixed. Fixing it would change what the live game writes, and this phase promised to change nothing. It is logged, named, and waiting for a decision.
>
> The substrate now has a nervous system. The model acts; the services notice.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_PHASE:** Phase 10 — Domain Event System (last planned OOA phase)
- **REFACTOR_TARGET:** `core/EventBus.groovy` (stub → live), `core/Player.groovy` (`capture`, `events`), `core/JournalManager.groovy` (listener; ritual removed), `core/RitualTracker.groovy` (new), `model/Room.groovy` + `model/NullSector.groovy` (→ `player.capture`), 13 import-only model files
- **SAFETY_GATE:** `JournalEventContractTest` (4 pins, new), `EventBusTest` (5, enabled), `JournalTest`, `AbyssalRitualTest`, `TracePersistenceTest`, 36 golden frames (unchanged), `./vinc.sh --scan` seed 0 → 9 nodes before and after, `DeterministicUniverseTest`

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [PHASE_10_DOMAIN_EVENTS]**

| Commit | Item | Change |
| :--- | :--- | :--- |
| `329d880` | 10-0 | `JournalEventContractTest`: room-option capture, `processAction` hidden frequency, `mergeItems` inside a building, NullSector echo — each pinned on `JournalManager.getRecentEvents` + `Building.sampledFloors`/`infusionCount`, all through `game.player`. Passed on master first. |
| `497c053` | 10a | `EventBus`: `LinkedHashMap<Class, List<Closure>>`, exact-class dispatch in subscription order, publish over a copy, unsubscribe by identity. `EventBusTest` `@Disabled` removed — skips 5 → 0. |
| `7185807` | 10b | `ItemCaptured`, `SynthesisPerformed` (`InventoryItem item`, nullable `Location location`; base `lip`/`itemName` derived). |
| `ee6a7df` | 10c-i | `GameState.events` (final). `Player(EventBus events = new EventBus())`. `PersistenceService.restore` and `SyncManager.restore(EventBus)` hand the state bus to the replacement player. |
| `e4fb3fb` | 10c-ii | `Player.capture(item, where)`; `mergeItems` publishes `SynthesisPerformed`. `JournalManager.attach(bus)` (two typed subscriptions, no location). `RitualTracker` holds the two ritual blocks verbatim. `Game` attaches journal, then tracker. |
| `25c5ada` | 10d | `Room` ×3, `NullSector` ×1 → `player.capture(item, this)`. `logCapture`/`logSynthesis` lose the `Location` parameter and the ritual blocks. No model class calls the journal. |
| `e1c9720` `eb46c28` `e3d0333` `7d7c788` | 10e-i..iv | Dead `import com.endlesstransit.core.JournalManager` removed from 13 model classes, four commits under the 4-file cap. `grep JournalManager model/` → 0. |
| `758c72c` | 10f | OOA plan Phase 10 record (three wrong premises, seven UNGUARDED rows, four declared edges E1–E4), `core/CLAUDE.md` events bullet, `model/CLAUDE.md` invariant 7, HK-010 + HK-011 opened, todo baseline. |
| `d02ac2e` | Merge | `--no-ff` to `master`. |

**Three premises the plan document had wrong:** `Building` never called the journal (import only); `logDiscovery` has no production caller since `7930dc3` (2026-03-05) → HK-010, behavior change, not done; the journal advanced the Abyssal ritual. **Events are two, not four:** `LocationEntered` waits on HK-010; `RitualCompleted` has no producer or consumer.

**Grill:** checks 1, 4, 5, 6 PASS; 2 and 3 AMEND → declared: E1 a `new Player()` outside `GameState` has an inert bus and no longer journals or advances the ritual (six test sites, none asserting either); E2 `Game.setPlayer` is test-only; E3 journal attaches before tracker (old write-then-ritual order); E4 `NullSector` passes itself, no `Building` above it. Re-verdict CLEARED.

**Execution note:** the 10d batch script asserted `Room` had an explicit `JournalManager` import — it uses `core.*`. The assert fired before the write; the pair was dropped and the script rerun. Suite arithmetic in the plan (204/199) was off: the five bus tests were already *discovered* as skipped, so enabling them moved 198 → 203 pass at 203 discovered.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 203 discovered / 203 pass / 0 skipped / 0 failed | Duration: ~3.1s
- **TRAVERSAL_DEPTH:** Phase 10 complete and merged. All ten planned OOA phases done; O1 (HeadlessRunner DSL) and O2 (CodeNarc) optional. Housekeeping open: HK-008, HK-009, HK-010, HK-011.
- **NEXT_PHASE:** user decision — HK-010 (restore discovery journaling: behavior change), O2 CodeNarc, or HK-008/011 (the two static singletons).

---
*End of Fragment. Neural Trace Stabilized.*
