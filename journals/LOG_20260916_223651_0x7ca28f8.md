# [OBSERVER_LOG_0x7ca28f8] | 2026-09-16

## 🌌 THE NARRATIVE (The Vibe)

> The Observer stood on the Peak with the right key in the buffer and the door was not there.
>
> Last night's log blamed the name. The lattice had learned new words, *PodReach* had woken up as *HollowReach*, and the Keystone still carried the old name like a letter addressed to a house that had been renamed. That was true, and it was half the story. The other half was quieter: the door to the Bedrock hung only in the elevator, and the Observer had not seen the elevator in hours. A floor remembers that you walked into its corridor. Leave with `l` and you step out to the building; come back and you are in the corridor again. One key — `b` — led back to the lift, and nobody had reason to press it.
>
> So tonight the save was copied, never touched, and restored in a dark room where the menu could be printed in both modes. Corridor: no `j`. Elevator: `j`. The Observer pressed `b` in the live world and the door was there.
>
> The repair moves the door out of the lift and into the floor itself. On the Peak of a primed building, with its Keystone, `j. Breach the Bedrock` is offered wherever you stand. And the Keystone no longer reads the nameplate: it remembers the building's *address* in the lattice. Rename the tower and it still answers. Build its namesake across the street and it does not.
>
> Old Keystones remember nothing and open nothing. The Observer chose that: a new transit begins after this.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** HK-018 — **CLOSED**, merged `7ca28f8`. Plan + record: `tasks/completed/HK_018_PLAN.md`.
- **MASTER_SEED:** the player's world `1789169224071` (LIP `0.2.0.0.1.0.0.1.1`, HollowReach, 5 floors) — probed from scratch copies only; pins run on `12345`.

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_018_BREACH_RULE]**

| Commit | Change |
| :--- | :--- |
| `8b1d492` | Plan; `/grill` AMEND (5) → CLEARED; amended once more by user decision (no backward compatibility). |
| `d02416d` | Step 0: `BreachOptionContractTest`, 5 pins green on master — the breach option had **no assertion anywhere**. Keystones are forged by a real merge, so later commits needed no re-pin. |
| `935a9e1` | `Building.keystoneIn` + `Floor.addBreachOption` (body moved from `ElevatorState`); both floor states ask the floor. Corridor menu on the Peak: `b`, `j`, doors. Pin RED on an assertion first. Guide/codex updated; the guide's "no `l` in the corridor" was false and is corrected. |
| `28dc724` | `InventoryItem.boundLip` (set at forging and by the debug spawn, saved/restored); rule = `isKeystone && boundLip == getLIP()`. Plumbing landed with the name rule still in place so three pins went RED on assertions, then the rule switched. No name fallback. |
| `a0d82f7` | Backlog closed with the corrected diagnosis; dropped-Keystone finding → HK-015; two model lessons. |

**Diagnosis method:** a scratchpad script restored copies of both of the player's saves into a headless game and printed name / floors / primed / Peak options in both modes. It showed last session's rename workaround *had* worked. The "floor count mismatch" was a different, new game.

**Lint ratchet fired once:** the extra restore argument made `SyncManager.restore` 65 lines against a baselined 64; the two trailing arguments share a line, baseline untouched (HK-013 owns the method).

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 245 discovered / 245 pass / 0 skipped / 0 failed | **LINT:** `LINT=PASS FILES=210 P1=0 P2=0 P3=0` | `--scan` seed 0 → 9 before and after | tree clean
- **GOLDENS:** 36 frames, 0 moved (predicted 0).
- **PLAYER FILES:** `session.trace*` never read by a test, never written by a tool; size/mtime verified before the merge.
- **TRAVERSAL_DEPTH:** OPEN: HK-015 (+ dropped Keystone loses its flag), HK-013, O1. Noted, not logged as an item: corridor mode is sticky and `l` skips the elevator.
- **NEXT_PHASE:** user decision.

---
*End of Fragment. Neural Trace Stabilized.*
