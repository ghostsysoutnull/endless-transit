# [OBSERVER_LOG_0xfe739f4] | 2026-09-16

## 🌌 THE NARRATIVE (The Vibe)

> The player came back to a world that was not theirs. They had asked to restore the last trace, and the lattice that answered was a stranger's — or no trace was found at all, and the void offered a fresh start nobody wanted.
>
> The log knew. Every time the suite ran, two persistence tests saved *their* worlds — seed 55555, seed 77777 — into the one file the player's game reads. And before every headless simulation, the runner deleted that file outright, "so the prompt would not hang". Six months of this. The file was gitignored, so no diff ever showed it. Only a player noticing surfaced it.
>
> The fix is a single question the game can now answer: *where is my trace?* By default, where it always was. In a test, a throwaway file in the temp directory that dies with the test. In a headless run, a path that does not exist, so the prompt never appears and nothing is deleted.
>
> And a promise, pinned: both persistence tests now photograph the player's file before they start and prove, at the end, that it is the same size, the same age, the same file. The whole suite ran twice across the merge. Fourteen thousand five hundred and ninety-three bytes, unchanged.
>
> Tests visit the substrate now. They no longer live in it.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** Housekeeping HK-012 — from a user report; found, planned, grilled, fixed and merged in one session
- **REFACTOR_TARGET:** `core/Game.groovy` (`String saveFile = SyncManager.SAVE_FILE`), `core/SyncManager.groovy` (`sync` reads `game.saveFile`; `restore(events, saveFile)`), `core/PersistenceService.groovy` (`restoreSession(saveFile)`)
- **SAFETY_GATE:** `TracePersistenceTest` + `CorridorPersistenceTest` (round trip through a temp file; real file's existence/size/mtime asserted unchanged), five `HeadlessRunner` tests, 36 goldens (untouched), `./vinc.sh --scan` seed 0 → 9
- **EVIDENCE:** `transit.log` 14:13:06 / 14:13:35 / 14:14:49 — `SYNC` + `RESTORE_INITIATED (Locus: 12FD1 / D903)` pairs from the suite between the user's sessions

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_012_SAVE_FILE_ISOLATION]**

| Commit | Item | Change |
| :--- | :--- | :--- |
| `a854684` | Plan | Root cause from the log and `git log -S`: tests writing the real save since `02b0748` (2026-03-05), runner deleting it since `25ad897` (2026-03-12). `/grill`: two citation ranges corrected → CLEARED. |
| `dc1d0a3` | Fix | `Game.saveFile`; `SyncManager.sync`/`restore` and `PersistenceService.restoreSession` use it; `QuitCommand`/`SyncCommand` unchanged. Tests: `File.createTempFile` + `finally` delete + before/after guard on the real file; `HeadlessRunner` points at a nonexistent temp path, deletes it in `finally`, deletes nothing else. `grep '"session.trace"\|trace.delete' src/test` → 0. |
| `faec18e` | Docs | Backlog HK-012 written CLOSED; `infrastructure.md` rule "a test never reads, writes or deletes a file the player owns"; todo/recovery; plan → completed. |
| `fe739f4` | Merge | `--no-ff` to `master`, pushed. |

**Verified:** `session.trace` `14593` bytes, mtime `1789586421` before the branch, after the fix commit's suite run, and after the post-merge suite run.

**Declared edges:** none for the player (same path, prompt, sync, restore). E3: a headless script that syncs writes to a temp file, deleted afterwards. E4: running the persistence tests no longer leaves a save behind for manual play — undocumented, never relied upon.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 207 discovered / 207 pass / 0 skipped / 0 failed | Duration: ~3.2s
- **TRAVERSAL_DEPTH:** HK-009, HK-010, HK-011, HK-012 closed today. Housekeeping open: HK-008 only. O1/O2 optional.
- **NEXT_PHASE:** user decision — O2 CodeNarc (re-plan as `./vinc.sh --lint`), HK-008, O1.

---
*End of Fragment. Neural Trace Stabilized.*
