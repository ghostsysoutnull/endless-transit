# [OBSERVER_LOG_0x3547535] | 2026-09-20

## 🌌 THE NARRATIVE (The Vibe)

> The substrate had rules it lived by and never wrote down in one place. One owner per fact. Behavior beside its data. Ask the object. Inject, never locate. A new kind is a registry entry. A thing is known by its key, not by what it is called. They were scattered across four domain charters and a drawer of lessons, each one filed under the accident that taught it — so the gates checked for the accidents and not for the rules, and the Operator was the only one still asking whether a new piece of the Lattice had the right shape.
>
> Today the eight rules stand in the Codex, each with the wave that taught it beside it. Six of them come with a question the interrogation must answer with evidence before a plan earns its authority; two are stated as guidance because their only measurable forms are proxies, and a gate on a proxy is a different argument. And the one shape a machine can see — a static method holding a rule with no owner — is now a tripwire in the lint, with the seventy-five it already tolerates named, each with its reason, and the list allowed only to shrink.
>
> The interrogation was run on paper against three old plans. It would have stopped all three at the planning line. The substrate asks the question earlier now, and cheaper.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** WF-009 (the shape of new code is a gate, not a review) — **COMPLETE** on `workflow/wf-009-shape-gate`, no `src/` change; id = last work commit `3547535`. Plan + execution record: `tasks/completed/WF_009_PLAN.md` (grill: AMEND ×2 → CLEARED).
- **MASTER_SEED:** N/A (law and tooling only).
- **User decisions:** options 1 + 2, not 3 (a post-implementation review pass); **D2:** the law states principles, not the two smells that happened — the grill asks only the questions that have a verifiable form.

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [WF_009_SHAPE_GATE]**

| Commit | Change | Evidence |
| :--- | :--- | :--- |
| `219fe4f` | plan + WF-009 logged; grilled | grill found the plan's static count wrong (90 with private helpers, 75 without) → rule exempts `private` |
| `18321cb` | **CODEX § 4 "OO Principles"** — eight rows (principle, taught by, check); **Shape Claim Protocol** (`what \| kind \| owner \| the one fact it owns \| statics + why`); `/grill` check 5 = deviations + pattern integrity + shape, referring to the table; still six checks | paper test: `HK_015_PLAN.md:63` (static `seedFrom`, principle 2), `:32` (`q` in `normalize`, principle 1), `HK_018_PLAN.md:35` (name-bound Keystone, principle 6) — three FAILs as written |
| `3547535` | **lint `NoNewStaticLogic`** — non-private static method with a body under `src/main` fails; allow-list of 16 files with a reason each (`Terminal` 41, `NameGenerator` 11 — the one real smell, → HK-022, …) | probe fires on `static int parse(` and `protected static`, silent on `private static` and `static final`; `LINT=PASS` on master, baseline untouched; baseline entries match one-for-one (masking worry unfounded — allow-list kept for its reasons) |

**Found while probing:** an `IllegalRegex` matches the whole file, so `^` needs `(?m)` — the first probe was silent; `grep -P` had verified the regex line by line. Rule: verify a lint regex against the *engine*, not against grep (retro-sized, no lesson — the "verify against the jar" lesson already covers it).

`/close-wave`: Light (no `src/`) + Chronicle (law and tooling changed). Also fixed: the recovery prompt's link table still listed HK-020 as OPEN after its own close-out.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 282 / 282 / 0 / 0 | `LINT=PASS` 220 files, 7 invariant rules, baseline 8 untouched | 36 goldens unchanged
- **NEXT_PHASE:** user decision — HK-021, HK-022 (`NameGenerator` statics), HK-013, O1, WF-006, the `KEYSTONE` question. Dogfood: the next plan carries a Shape table.

---
*End of Fragment. Neural Trace Stabilized.*
