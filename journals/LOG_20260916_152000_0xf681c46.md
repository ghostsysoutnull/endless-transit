# [OBSERVER_LOG_0xf681c46] | 2026-09-16

## 🌌 THE NARRATIVE (The Vibe)

> Every world in the lattice was built by the same hand. One builder, in one global slot, and each game that woke up reached into that slot and swapped in its own voice before the first street was drawn. It worked because only one game ever woke at a time. In the test substrate, forty-seven of them wake in a row, and each one overwrote the voice of the one before.
>
> Nobody heard it. The instruments were tuned to the last speaker. A scanner found a landmark on seed 100 and rendered its street in bold cyan — through a formatter it had never been given, left behind by whichever game had spoken last. The test passed. It had been passing by accident since the scanner was written.
>
> Now each game carries its own builder, made with its own voice at birth, and hands it to the two services that need to raise a world: the one that walks the player in, and the one that brings a saved trace back. Every place the builder creates remembers who built it. When a floor asks for its corridor, when a breached tower opens a layer below zero, when a restored universe unfolds around a saved room — the request goes back to the builder that made the parent, never to a slot.
>
> A place built by hand, with no builder, says so the first time anyone asks for its children. It names its own kind. The old failure was a null somewhere inside population; the new one is a sentence.
>
> The last static singleton is gone. Phase 5 took the formatter out of the global; this took the world. The housekeeping backlog is empty for the first time since Phase 7 logged its first item. The substrate has no globals left to reach for — only things that were handed in.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** Housekeeping HK-008 — `ProceduralFactory.instance` (static since the factory existed) → an instance owned by `Game`
- **REFACTOR_TARGET:** `procgen/ProceduralFactory.groovy` (constructor takes `fmt`, `wire()` stamps `factory` on every container it hands out, static deleted), `model/Container.groovy` (`factory` + fail-loud `populateChildren`), `model/Building.groovy`, `core/Game.groovy` (`final factory`), `core/NavigationOrchestrator.groovy`, `core/PersistenceService.groovy`, `core/SyncManager.groovy`, `procgen/WorldGenesis.groovy` (factory parameters), `procgen/SeedScanner.groovy` (own factory)
- **SAFETY_GATE:** `FactoryWiringContractTest` (new: A fmt identity — step 0, passed on `master`; B factory identity + fail-loud; C ownership, two games never share), `DeterministicUniverseTest`, `ProcgenSnapshotTest`, `ProcgenDeepSnapshotTest`, `TracePersistenceTest`, `AbyssalRitualTest`, `ProceduralFactoryRegistryTest`, 36 goldens, `./vinc.sh --scan` seed 0 → 9

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_008_FACTORY_INJECTION]**

| Commit | Item | Change |
| :--- | :--- | :--- |
| `2d6978e` | Plan | Both trees grepped (7 production readers / 9 sites; 11 test readers / 19 sites; 11 hand-construction tests). `/grill`: check 4 AMEND — the first c4 dropped `Game`'s write into the static while `SeedScanner` still generated through it, and `LandmarkDiscoveryTest:43` renders a scanner-built street → the scaffold line stays through c4. CLEARED. |
| `ad80a18` | Step-0 pin | `FactoryWiringContractTest` A1/A2: every node Universe → Room carries `game.fmt`; a restored world carries the restoring game's `fmt`. UNGUARDED before (rendering only proved non-null). |
| `d3cd6f2` | c3 | `ProceduralFactory(OutputFormatter)`; `wire()` on 13 container delegators; `Container.factory` + `populateChildren()` throws `IllegalStateException` naming the class when it is null; `Building` uses `factory`; `Game.getFactory()` facade-first. Guard fired in 18 tests / **12** files (plan said 11): `SurvivalPinningTest`'s hand-built Apartment parent and two hand-built parents (`StreetTest` City, `NavigationSyncTest` Street) are populated through `getIndexInParent()`. Part B pins. |
| `d3d7d66` | c4 | `Game.factory` final, built with `fmt`; injected into `NavigationOrchestrator` and `PersistenceService`; `WorldGenesis.createInitialWorld(factory, locus)`, `SyncManager.restore(factory, events, saveFile)`. Part C pins (C1 restore, C2 two games). |
| `2d3cac3` | c5 | Static deleted; `fmt` final; `SeedScanner` owns `new ProceduralFactory(new StandardTerminalAdapter())`; 11 test readers build or borrow a factory. `grep ProceduralFactory.instance src/` → 0. |
| `073d803` | Docs | Backlog HK-008 CLOSED (backlog empty); `procgen`/`model`/`core` invariants; `model.md` lesson (parents populate via `getIndexInParent`; what a locator hides); todo/recovery; plan record. |
| `f681c46` | Merge | `--no-ff` to `master`. Suite rerun on master: 213/213. |

**Declared deviations:** back-reference stamped by the facade (one file) rather than by the 14 per-type factories — every child passes through the delegators (zero direct per-type callers, grepped); `SeedScanner` gets a real adapter where it used to inherit the static's `fmt` or null; one `ThemeService` per factory (38 small classpath reads per `new Game` — suite 2.97–3.33 s across four runs against a 2.99 s baseline: below noise); `GameState` untouched (a service is not state; Phase 6 precedent is constructor injection into services).

**Finding:** `LandmarkDiscoveryTest` passed only by test order before this change (its street's `fmt` was whatever the last `Game` had written into the static). Recorded in `tasks/lessons/model.md`.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 213 discovered / 213 pass / 0 skipped / 0 failed | Duration: ~3.2s
- **TRAVERSAL_DEPTH:** HK-005 … HK-012 all closed; the housekeeping backlog is empty. All ten OOA phases complete. `grep -rn "static .* instance" src/main` → 0.
- **NEXT_PHASE:** user decision — O2 CodeNarc (re-plan as `./vinc.sh --lint`) or O1 HeadlessRunner DSL.

---
*End of Fragment. Neural Trace Stabilized.*
