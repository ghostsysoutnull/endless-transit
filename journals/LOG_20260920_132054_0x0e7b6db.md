# [OBSERVER_LOG_0x0e7b6db] | 2026-09-20

## 🌌 THE NARRATIVE (The Vibe)

> Two ledgers said what a word from the Operator meant. One sat at the ear of the link and lowered every whisper it recognised; the other sat at the switchboard and held the actual wires. They agreed, mostly, because someone kept them agreeing by hand. A new wire added to one and not the other would still work — but only if you shouted it in exactly the right case, and nobody would know why.
>
> Today the ear stopped keeping a list. It does two things now, and only two: silence on the line ends the link, an empty line repeats the last step. Every other word goes to the switchboard as typed, and the switchboard owns the whole truth — which words, which shorthands, which ones must be typed exactly (`p` and `P` are two different acts; `s`, `ll` and `quitnow` have always been precise). A side door that tests used, which had skipped the ear entirely, now hears the shorthands too.
>
> Nothing the Operator can type answers differently. The substrate feels the same from the chair. It just has one owner for the fact it used to have two.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_TASK:** HK-020 (global command aliases have two owners) — **COMPLETE**, merged `0e7b6db`. Plan + execution record: `tasks/completed/HK_020_PLAN.md` (grill: AMEND ×2 → CLEARED).
- **MASTER_SEED:** `12345` (`GlobalCommandsContractTest` game rows); no world change.
- **User decisions:** D1 = a (one `GlobalCommands` object; `dispatch` asks it — declared deviation from the backlog's "`normalize` asks it"), D2 = keep every case quirk exactly as today.

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_020_GLOBAL_COMMANDS]**

| Commit | Change | Pins |
| :--- | :--- | :--- |
| `46fee07` | plan, grilled (deviation declared; `CoherenceDrainTest:19` listed as the second `TurnProcessor` constructor caller) | — |
| `216545c` | step 0: today's `normalize` contract pinned — six any-case words, exact keys pass through, EOF → quit, empty → repeat | `InputHandlerNormalizeTest` 271 → 275, green on master |
| `3119693` | `GlobalCommands` (`register(key, cmd, anyCase = true)`, `alias`, `resolve`); `TurnProcessor.initializeGlobalCommands` builds it, `dispatch` calls `resolve`; `normalize` keeps EOF/empty only | `GlobalCommandsContractTest` ×10 — `helpAliasReachesHelpCommandThroughDispatch` RED before the change (`processInput("?")` fell to navigation); c0's alias/case rows moved one for one. 282 / 282 |
| `b5e8941` | guide citations (`:103-104`), `TurnProcessor` blueprint → `Verified`, `core/CLAUDE.md` + `GEMINI.md` invariant line, backlog CLOSED, plan → `completed/` | — |

**Deviation, recorded in c1:** `ll` is registered exact against `lattice`'s instance instead of `alias()`-ed — `alias` inherits the target's any-case rule and D2 keeps `ll` exact. **Left open (declared):** `RenderingCoordinator.helpMenu:40` still spells `map/m`, `ll/lattice`, `q` by hand — display text, not a resolver.

`/close-wave`: Full (`src/` changed), no add-ons.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 282 / 282 / 0 / 0 (271 + 11 pins) | `LINT=PASS` 220 files, baseline 8 untouched | scan seed 0 → 9 | 36 goldens unchanged
- **NEXT_PHASE:** user decision — HK-021, HK-013, O1, WF-006, the `KEYSTONE` question, optional lessons diet.

---
*End of Fragment. Neural Trace Stabilized.*
