# [OBSERVER_LOG_0x9549527] | 2026-09-20

## 🌌 THE NARRATIVE (The Vibe)

> Every place in the lattice has a name, and until today no one owned the voice that spoke them. The names came
> from nowhere — a set of words hanging in the void above the substrate, reachable from anywhere, belonging to
> nothing. The lexicon loaded itself the instant the class was first glimpsed, before any world existed to need it.
>
> It worked. That was the problem with it. A voice with no body cannot be replaced, cannot be given to one world
> and withheld from another, cannot be asked who it answers to. The Shape Gate had already marked it: fifteen
> entries on the allow-list were tools and instruments, and one was a rule with no owner.
>
> So the voice was given a body. Each registry now builds its own namer, exactly as it builds its own theme
> service, and the eleven factories that name things ask the registry instead of the sky. The move was made one token at a time —
> the callers first, while the old statics still answered, and only then the statics themselves, lifted by script
> with every line above the two-hundredth left where the player's guide expects to find it.
>
> Nothing the traveller sees has moved. Hydraia is still Hydraia; the Eternal Shaft still stands on Busy Terrace.
> Seven new pins hold the branches nobody had watched — the landmark roll, the `Unit 0x…` blocks, the quiet
> fallback to monolith — and they crossed the change without a single edit.
>
> The substrate feels the same and is not. There is one fewer thing in it that simply *is*; everything that names
> now belongs to someone.

---

## ⚙️ THE SUBSTRATE (The Tech)

### 📍 LOCUS_CONTEXT
- **ACTIVE_WAVE:** HK-022 — `NameGenerator` is eleven static generators (housekeeping, found by the WF-009 allow-list review)
- **REFACTOR_TARGET:** `procgen/NameGenerator.groovy` → instance owned by `ProceduralFactory.nameGenerator`
- **SAFETY_GATE:** `ProcgenSnapshotTest`, `ProcgenDeepSnapshotTest`, `NameGeneratorContractTest` (new), 36 goldens, `--scan` seed 0
- **PINNED SEED:** `0x1234` (all name literals)

### 🛠️ IMPLEMENTATION_DELTA

**Session label: [HK_022_NAME_GENERATOR]** — merge `9549527`, branch `housekeeping/hk-022-name-generator`, plan + record `tasks/completed/HK_022_PLAN.md`.

| Commit | Item | Change |
| :--- | :--- | :--- |
| `bb35045` | plan | Grilled: AMEND ×3 (one assertion citation, two undeclared edges — per-factory lexicon load, guide line citations — helpers clarified) → CLEARED. |
| `d44a5aa` | c0 pins | `NameGeneratorContractTest`, 7 rows on an *instance* (Groovy resolves a static through one): sector + solar-system literals, landmark, both uncommon building templates, null-adjective room, unknown-culture fallback, two generators agree. 289/289. |
| `b58ec1f` | c1a | `ProceduralFactory`: `final NameGenerator nameGenerator = new NameGenerator()` beside `themeService`. |
| `5d4c1ea` | c1b | Filament/Sector/SolarSystem/Planet/Country factories → `registry.nameGenerator` (one token each, by script, reverse-checked). |
| `9dac3ef` | c1c | City/Street/Building/Corridor/Apartment factories → same. |
| `d27ad3e` | c1d | Allow-list entry removed first → `LINT=FAIL P3=11` (RED demo); `RoomFactory` moved; 10 public statics + 3 private helpers + the class-init lexicon de-staticised by script (reverse substitution reproduces the original; lines 1–199 unmoved); never-called `generateContainerName` deleted (D1 = a; `git log -S` shows only its adding commit); three test files use the instance; wiring pin `C3_eachFactoryOwnsItsNameGenerator`. |
| `edcc9e6` | c2 docs | `NameGenerator.md` rewritten from source, `ProceduralFactory.md` corrected — both `Verified`; `procgen/CLAUDE.md` names the owner. |

- **[SHAPE]** First wave through the WF-009 Shape table: `NameGenerator` = service, owner `ProceduralFactory`, one `static final` constant (`landmarkTitles`). `NoNewStaticLogic` allow-list 16 → 15; every remaining entry is tooling, formatting or an entry point.
- **[PROBE]** `@CompileStatic` accepts `registry.nameGenerator.generateX(...)` while `generateX` is still static — that is what let 13 production files move in four green commits instead of one red one.
- **[DECLARED EDGES]** `[THEME_WARN]` still goes through static `Terminal.println` (same as `ThemeService`); generators still return `Map<String,Object>`; the warn *line* is not pinned (no capture seam), only the fallback result.
- **[SIDE FINDING]** A raw small `LocusSeed(n)` never rolls under ~0.2 on its first `nextDouble` (3,000 seeds, all "common" buildings); production loci are always branched, so the world is unaffected — test authors must branch. The guide's three `NameGenerator.groovy` line citations had already drifted before this wave → logged under HK-023.

### 📈 ENTROPY_REPORT
- **SUITE_STATE:** 290 discovered / 290 pass / 0 skipped / 0 failed | Duration: ~4.5 s (baseline 4.96 s — the per-factory lexicon load is not measurable)
- **GATES:** `LINT=PASS FILES=221`, baseline 8 unchanged; `--scan` seed 0 → 9; 36 goldens byte-identical; `DOCS=PASS` at close.
- **TRAVERSAL_DEPTH:** Housekeeping backlog OPEN: HK-021, HK-023, HK-013.
- **NEXT_PHASE:** user decision — see `tasks/RECOVERY_PROMPT.md` **Next**.

---
*End of Fragment. Neural Trace Stabilized.*
